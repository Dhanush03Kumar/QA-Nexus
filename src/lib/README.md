# Lib Directory

Library code including database configuration, services, and utility functions.

## Structure

- `database/` - Dexie.js setup and database schema
- `services/` - Service modules for data access
- `utils.ts` - Shared utility functions